---
date: 2026-09-08
---

# Devlog 2026-09-08

<!-- 最新的条目在最上面。待办与未决项不写在这里，见根目录 STATUS.md。 -->

## 02:19 · 定实验落地路径为 Project/<名>/，同步 GitHub 仓库名 Platform

```yaml
type: arch
scope: 仓库结构 / remote
status: done
files:
  - STATUS.md
```

### 改动

- 删除 STATUS.md 中「新实验代码落地路径」未决项；约定为 `Project/<名>/`。
- 本地 `origin` 从 `https://github.com/SimonYanle/Project.git` 改为 `https://github.com/SimonYanle/Platform.git`（与 GitHub 上已改名的仓库对齐）。
- 本地根目录文件夹名此前已改为 `E:\Platform`，并新建空目录 `Project/`。

### 决策与权衡

- **选择 `Project/<名>/`**：与本地已建目录一致，避免再引入 `experiments/` 或根目录散放子项目。
- **放弃 `experiments/<名>/`、根目录 `<名>/`、按月分**：前两者与现有文件夹命名冲突或会污染仓库根；按月分对「一个可独立演进的项目」语义不匹配。
- **remote URL 显式改写**：GitHub 对旧名有重定向，但本地仍显示 `Project.git` 容易误导后续协作，改为与正式名一致。

### 验证

- 命令：`gh repo view SimonYanle/platform --json name,url`；`SimonYanle/Project` 亦解析到同仓。
- 结果：正式名为 `Platform`，URL `https://github.com/SimonYanle/Platform`。
- 命令：`git remote set-url` 后 `git remote -v`（见本轮终端）。
