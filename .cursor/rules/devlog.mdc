---
description: Devlog 记录规范：重要变更完成后写入 devlog/YYYY-MM-DD.md，必须记录决策与权衡
alwaysApply: true
---

# Devlog 规则

重要变更完成后，把这一轮的思考过程写进 `devlog/<YYYY-MM-DD>.md`。新条目插入到文件顶部（`# Devlog <日期>` 标题之后），最新的在最上面；当天文件已存在就不要新建，也不改写历史条目。

待办、未决项、观察项不写进 devlog，写进根目录 `STATUS.md`（可随时改写，完成即删除）。

## 判断是否需要记录

两条判据满足任一即记录：

1. **类型判据**：属于架构或技术选型决策、新功能、Bug 修复、踩坑与绕过方案、方案推翻重来、心智模型转变、阶段复盘。
2. **价值判据**：这一轮产生了下次会用到的判断，即使没改动代码也算（如纯讨论定下方案）。

两条都不满足才跳过：错别字、格式化、纯注释、无决策含量的机械改动，且没产生任何判断。

只满足类型判据、没有判断含量的改动，记录要写短：只留「改动」小节，其余删掉，不要为了填满模板而编造决策。

## 条目格式

复制 `devlog/_TEMPLATE.md` 的结构：`## HH:MM · 一句话摘要` 标题，紧跟一个 YAML 元信息块（`type` / `scope` / `status` / `files`），再是 markdown 小节。

小节按实际情况取舍，但「决策与权衡」只要有决策就必须写：

- **改动**：做了什么，涉及哪些文件。
- **决策与权衡**：为什么这么做，考虑过哪些替代方案，为什么放弃它们。
- **验证**：跑了什么命令或测试，输出是什么。
- **卡点与修复**：现象、根因、解法。
- **心智转变**：原以为 X，实际是 Y。

## 写入时机

在改动完成后、给用户做汇报之前写。顺序反过来会让 devlog 退化成汇报的复印件，失去增量价值。

一轮工作写一条，不要拆成多条碎记录。

提交时有 `.githooks/pre-commit` 兜底检查当天 devlog 是否一并更新，但它只验证记录存在，不验证记录有价值——不要为了过关而写空记录。

## 硬性要求

- 只写实际发生的事。计划中的事放 `STATUS.md`，不能写成已完成。
- 没验证的不能写成已验证，写「未验证」。
- 有决策就必须写「为什么」，不能只写「改了什么」。
- 用中文；日期时间用本地时间。
