# AGENTS.md

本文件是本项目的协作约定，对所有 AI 会话生效。

### 1. 记录 Devlog（重要变更必写）

完成一次**重要变更**后，把这一轮的思考过程写入 `devlog/<YYYY-MM-DD>.md`。

- 新条目**插入到当天文件顶部**（`# Devlog <日期>` 标题之后），最新的在最上面；当天文件不存在则新建。不改写、不删除历史条目。
- 条目格式见 `devlog/_TEMPLATE.md`：`## HH:MM · 一句话摘要` 标题 + YAML 元信息块 + markdown 小节。
- **决策必写**：为什么这么做、考虑过哪些替代方案、为什么放弃。只写「改了什么」不算合格记录。
- 同时记录真实发生的：踩的坑、根因、修复方案、心智模型的转变、复盘结论。
- 验证要如实写。跑过的命令写命令和结果；没验证就写「未验证」，不能写成已验证。
- 只写实际发生的事，不写计划中的事。待办、未决项、观察项一律写进根目录 `STATUS.md`，不写进 devlog。

**判定标准（两条判据满足任一即记录）**：

1. **类型判据**：改动属于这些类型之一——架构或技术选型决策、新功能、Bug 修复、踩坑与绕过方案、方案推翻重来、心智模型转变、阶段复盘。
2. **价值判据**：这一轮产生了一个下次会用到的判断，即使没有改动任何代码也算（例如纯讨论定下了一个方案）。

**两条都不满足才跳过**：错别字、代码格式化、纯注释调整、无决策含量的机械改动，且没有产生任何判断。

取「或」的取舍：好处是判断类的收获不会因为「不属于清单里的类型」而漏掉；代价是类型符合但没有判断含量的改动也会留下记录。**这类记录要写短**——只留「改动」小节，其余小节直接删掉，绝不为了填满模板而编造决策。

**触发方式**：

- **开发中**：由 `.cursor/rules/devlog.mdc`（始终生效）约束，在改动完成、给出汇报之前写入。
- **提交时**：由 `.githooks/pre-commit` 兜底。本次提交若改动了实质文件而当天 devlog 未一并 `git add`，提交会被拦下。只改 `devlog/`、`AGENTS.md`、`STATUS.md`、`.cursor/`、`.githooks/` 时不触发。
- 确实无需记录时跳过：`git commit --no-verify`，或设 `SKIP_DEVLOG=1`。
- 钩子只能检查记录是否存在，检查不了记录是否有价值。内容质量靠上面的判定标准，不靠钩子。

本机已配置完毕。重新克隆仓库后需要执行一次：

```powershell
git config core.hooksPath .githooks
```

完整规则见 `.cursor/rules/devlog.mdc`（始终生效）。

### 2. 维护 STATUS.md

根目录 `STATUS.md` 记录当前待办、未决问题与观察项。

- 它和 devlog 的分工：**devlog 记已发生的事，只追加不改写；STATUS.md 记尚未发生的事，随时改写。**
- 产生新的待办或未决项时写进去；事项完成后**直接删掉**，不保留「已完成」清单——它的来历在 devlog 里查得到，留在 STATUS.md 里只会让文件越来越难读。
- 改动后更新文件顶部的「最后更新」时间。

## 沟通约定

- 用中文回复。
- 动手前先说清要做什么；做完说清改了哪些文件、怎么验证的、结果如何。

